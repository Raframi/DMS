@model IEnumerable<DMSModels.Models.KeywordDataType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KeywordDataTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KeywordDataTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KeywordDataTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.KeywordDataTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KeywordDataTypeId })
        </td>
    </tr>
}

</table>
